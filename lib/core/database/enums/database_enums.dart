enum TransactionType {
  expense,
  income,
  transfer,
}

enum BudgetPeriod {
  weekly,
  monthly,
  yearly,
}

enum RecurrenceFrequency {
  daily,
  weekly,
  monthly,
  yearly,
}

enum PaymentMethod {
  card,
  cash,
  account,
}
